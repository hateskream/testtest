server {
  listen 8082;
  server_name localhost;

  location / {
    root /usr/share/nginx/html;
    try_files $uri $uri/ /index.html =404;
  }

  location /assets {
    root /usr/share/nginx/html;
    try_files $uri $uri/ =404;
  }
}
