install_dependencies:
  stage: setup
  image:
    name: node:24.4.1-alpine
    pull_policy: if-not-present

  script:
    - npm ci

  artifacts:
    paths:
      - node_modules/
    expire_in: 1h

  rules:
    - if: $SKIP_ALL_JOBS == "true"
      when: never
    - when: on_success
