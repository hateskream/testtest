#### 1. Контекст и цель

**Зачем этот MR? Какую проблему решает?**

- Кратко опиши бизнес-контекст или техническую причину изменений
- Ссылка на задачу / тикет: `JIRA-123` / `#456`

#### 2. Что сделано

**Фактические изменения (bullet points):**
- 1
- 2
- ...
> Формулировки уровня _«переписал логику X на composable»_, а не _«поправил код»_

#### 3. Как это работает сейчас

**Поведение системы после изменений:**

- Основной пользовательский сценарий:
- Edge cases (empty / error / loading):
- Что изменилось по сравнению с прошлым поведением:

### Чек лист проверки

#### 1. **Функциональность и корректность**
   - [ ] Код делает то, что задумано? Соответствует ли требованиям задачи?
   - [ ] Работает ли в edge cases (граничные значения, пустые входы, null)?
   - [ ] Правильно ли обрабатываются ошибки и исключения (try-catch, validation)?
   - [ ] Нет ли регрессий? (Не сломал ли код существующий функционал.)

#### 2. **Читаемость и стиль кода**
   - [ ] Понятны ли имена переменных, функций, классов и методов? (Избегайте аббревиатур вроде `tmp`, используйте descriptive names.)
   - [ ] Код лаконичен? Нет ли дублирования (DRY-принцип)?
   - [ ] Легко ли понять логику без глубокого погружения?
   - [ ] Удален ли мертвый код?

#### 3. **Производительность и эффективность**
   - [ ] Нет ли неоптимальных операций (N+1 queries, лишние циклы, memory leaks)?
   - [ ] Эффективно ли используется ресурсы (CPU, память, сеть)?
   - [ ] Deep watch без необходимости
   - [ ] Виртуализация больших списков
   - [ ] Тяжёлые библиотеки грузятся асинхронно и только там где надо?

#### 4. **Архитектура и дизайн**
   - [ ] Код модульный? Легко ли расширять/поддерживать?
   - [ ] Чёткое разделение слоёв
   - [ ] Fat component (script + template + style > 1к срок)
   - [ ] Нет ли over-engineering (слишком сложных решений для простой задачи)?
   - [ ] Корректность ключей в tan-stack-query (нет ли явной ошибки)
   - [ ] Правильно ли выбраны taleTime, cacheTime в tan-stack-query

#### 7. **Документация и комментарии**
   - [ ] Обновлена ли документация (README, API docs, inline-комментарии)?
   - [ ] Комментарии объясняют "почему", а не "что" (код должен быть self-documenting)?
   - [ ] Нет ли избыточных или устаревших комментариев?

#### 8. **UI**
   - [ ] UI-состояния полностью покрыты (loading / empty / error / success)
   - [ ] Тротлинг/дебаунс операций которые происходят часто
   - [ ] Синхронизация URL <-> состояние, восстановление состояния (если уместно в контексте задачи)

#### 9 **MR-гигиена**
- [ ] Есть ли попутные рефакторинги? (Если задача продуктовая не должно быть, если нет не должно быть)
- [ ] Есть ли форматирование чужого кода? (Если задача продуктовая не должно быть, если нет не должно быть)
- [ ] Соблюдается ли правило бойскаута? (Если я залазию в ходе работы в чужой код, после меня чище чем до меня)
- [ ] Имеется ли заполненый дескрипшен в MR?
